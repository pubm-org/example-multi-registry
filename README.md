# example-multi-registry

pubm example: publishing a single package to both npm and jsr simultaneously.

## Configuration

See [pubm.config.ts](./pubm.config.ts) for the configuration. Key setting: `registries: ["npm", "jsr"]`.
